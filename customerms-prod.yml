cassandra:
  contact-points: localhost
  port: 9042
  keyspace-name: customer_domain
eureka:
  client:
    service-url :
      defaultZone : http://localhost:8761
    register-with-eureka: false
    fetch-registry: false
